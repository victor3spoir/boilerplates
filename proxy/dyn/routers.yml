http:
  routers:
    router0:
      entryPoints:
      - http
      middlewares:
      - my-basic-auth
      service: traefik-svc@docker
      rule: Host(`homelan.net`)
    # router1:
    #   entryPoints:
    #   - http
    #   middlewares:
    #   - my-basic-auth
    #   # service: service-foo
    #   rule: Host(`one.homelan.net`)