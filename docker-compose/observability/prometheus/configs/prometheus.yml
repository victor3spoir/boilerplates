global:
  scrape_interval: 5s
  evaluation_interval: 5s
  scrape_timeout: 5s

remote_write:
  - url: http://alloy-srv:9090/api/v1/metrics/write # http://mimir:9009/api/v1/push

rule_files:
  - rules.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

scrape_configs:
  # - job_name: prometheus-self
  #   static_configs:
  #     - targets:
  #         - localhost:9090
  #       labels:
  #         app: prometheus
  #   basic_auth:
  #     username: username
  #     password: password

  - job_name: traefik
    metrics_path: /metrics
    scrape_interval: 5s
    static_configs:
      - targets:
          - traefik:8082
      # basic_auth:
      #   username: username
      #   password: password
      # authorization:
      #   type: Bearer
      #   credentials: 
      # credentials_file: 
