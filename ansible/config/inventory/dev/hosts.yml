all:
  vars:
    ansible_user: automation
    ansible_become: true
    # ansible_ssh_private_key_file: /root/.ssh/private_key
  children:
    all_servers:
      hosts:
        srv-dev:
          # ansible_host: srv-dev.mshome.net
          ansible_host: srv-dev.multipass
