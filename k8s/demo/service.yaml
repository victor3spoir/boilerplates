apiVersion: v1
kind: Service
metadata:
  name: deamonset-svc
  namespace: demo
spec:
  selector:
    app: deamonset
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000