# DB
POSTGRES_PASSWORD=

# NEXTCLOUD
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=password
NEXTCLOUD_TRUSTED_DOMAINS=localhost,local.host,
SMTP_SECURE=true
SMTP_HOST=inbucket
SMTP_PORT=2500
SMTP_AUTHTYPE=LOGIN #LOGIN/PLAIN
SMTP_NAME=username
SMTP_PASSWORD=password
MAIL_FROM_ADDRESS=admin@local.host
MAIL_DOMAIN=local.host
PHP_MEMORY_LIMIT=512M
PHP_UPLOAD_LIMIT=512M